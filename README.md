# Binary-Clock-Using-Arduino-
An Arduino-based binary clock using a DS3231 RTC module and LEDs to display hours, minutes, and seconds in binary format. The system updates every second and uses bitwise operations to control the LEDs.
